전용 편집기 다운로드
editplus. chrome

*교사PC IP == 공유폴더 들어가기
window키 + R  70.12.112.90

*태그 공부 사이트
w3schools.com -> tutorials -> html reference -> tag list

* 컴퓨터 이름 설정
:제어판->시스템 및 보안->시스템->고급 시스템 설정->컴퓨터 이름

*설치 폴더는 c드라이브에 저장하는 것이 좋다.

*dir
:현 디렉토리에 저장되어있는 데이터 리스트 출력

*cls
:화면을 지웁니다.

*cd .. 
상위디렉토리로 이동

*cd 이동할 디렉토리

*쿼리문(mysql)문은 대소문자 가리지 않는다.
	data는 대소문자를 가린다.

*직계와 통계
select sum(SAL) from emp;
select min(SAL) from emp;
select max(SAL) from emp;]


*커맨드 창 캡처
spool '캡처.txt.를 저장할 디렉토리 경로.제목.txt'
spool off

*선생님 블로그
zino1187@naver.com

*급여가 높은 순으로 출력(내림차순)
select * from emp order by sal desc; <-> asc;

* -- 주석

*표가 가독성이 없을 떄
set line 200
set pagesize 20;